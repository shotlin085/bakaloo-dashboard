/**
 * Vitest global test setup.
 *
 * Registers `@testing-library/jest-dom` custom matchers (e.g.
 * `toBeInTheDocument`, `toHaveTextContent`) on the global `expect`.
 */

import "@testing-library/jest-dom/vitest"
